---
interface Props {
  filled?: boolean
  outlined?: boolean
  white?: boolean

  rounded?: boolean
  centered?: boolean

  big?: boolean
  small?: boolean

  disabled?: boolean
}

const { 
  filled, 
  outlined,
  white, 
  
  rounded,
  centered,
  
  big, 
  small,

  disabled
} = Astro.props

---
<button 
  class:list={[ 
    'button', 
    filled && 'button_filled', 
    outlined && 'button_outlined', 
    white && 'button_white',
    rounded && 'button_rounded', 
    centered && 'button_centered',
    big && 'button_big',
    small && 'button_small' 
  ]}
  disabled={disabled}
>
  <slot />
</button>